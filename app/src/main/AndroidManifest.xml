<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <queries>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE"/>
        </intent>
    </queries>

    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuanLyChiTieu"
        tools:targetApi="31" >

        <activity
            android:name=".view.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".view.LoginActivity"
            android:exported="false" />

        <activity
            android:name=".view.RegisterActivity"
            android:exported="false" />

        <activity
            android:name=".view.HomeActivity"
            android:exported="false" />

        <activity
            android:name=".view.ProfileActivity"
            android:exported="false" />

        <activity
            android:name=".view.TransactionActivity"
            android:exported="false" />

        <activity
            android:name=".view.MonthlyStatsActivity"
            android:exported="false" />

        <activity android:name=".view.HistoryActivity" />

        <activity android:name=".view.ReportActivity" />
        <activity android:name=".view.ReportHistoryActivity" />
        <activity android:name=".view.CsvViewerActivity" />

        <activity
            android:name=".view.StatsActivity"
            android:exported="false" />

        <activity
            android:name=".view.CategoryDetailActivity"/>
        <activity android:name=".view.SavingGoalActivity"
            android:exported="false"/>

        <activity android:name=".view.AddSavingGoalActivity"
            android:exported="false"/>

        <activity android:name=".view.EditSavingGoalActivity"
            android:exported="false" />

        <activity android:name=".view.NotificationActivity"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">

            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>